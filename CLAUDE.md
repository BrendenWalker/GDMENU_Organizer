@AGENTS.md

## Claude-specific

Never pass `--trailer` to `git commit`, and never add `Co-authored-by`, `Made-with`, or similar attribution to commits or pull requests. Follow `.AI/commit-and-pr-style.md`.
